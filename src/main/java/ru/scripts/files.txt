feature - files
save files to db, get, process and etc.

create a repository with attributes - 
file_type varchar2(30)
file_desc varchar2(300)
product_code 
file_body blob

create service and api controllers

POST /admin/files/ in body - json { file_type, file_description, product_code }
save it to repository
return json { id, file_type, file_description, product_code }

POST /admin/files/{id} in body file_body
update repository with ID, save file to table column

DELETE /admin/files/{id} set flag is_deleted to true

GET /admin/files?product_code=*** return array of {id, file_type, file_description, product_code }

GET /admin/files/{ID} return file body 

POST /admin/files/{ID}/cmd/process in body json with key-value array [{ key: aaa, value: bbb}]
service method get file from db by ID
it is a PDF file
make a loop by array from request body and set value in pdf form by name
save pdf form as pdf and return it in responce

